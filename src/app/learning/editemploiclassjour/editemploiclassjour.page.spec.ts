import { ComponentFixture, TestBed } from '@angular/core/testing';
import { EditemploiclassjourPage } from './editemploiclassjour.page';

describe('EditemploiclassjourPage', () => {
  let component: EditemploiclassjourPage;
  let fixture: ComponentFixture<EditemploiclassjourPage>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(EditemploiclassjourPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
