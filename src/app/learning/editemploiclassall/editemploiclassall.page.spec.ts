import { ComponentFixture, TestBed } from '@angular/core/testing';
import { EditemploiclassallPage } from './editemploiclassall.page';

describe('EditemploiclassallPage', () => {
  let component: EditemploiclassallPage;
  let fixture: ComponentFixture<EditemploiclassallPage>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(EditemploiclassallPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
