import { ComponentFixture, TestBed } from '@angular/core/testing';
import { EditemploienseignjourPage } from './editemploienseignjour.page';

describe('EditemploienseignjourPage', () => {
  let component: EditemploienseignjourPage;
  let fixture: ComponentFixture<EditemploienseignjourPage>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(EditemploienseignjourPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
