import { ComponentFixture, TestBed } from '@angular/core/testing';
import { EditemploienseignallPage } from './editemploienseignall.page';

describe('EditemploienseignallPage', () => {
  let component: EditemploienseignallPage;
  let fixture: ComponentFixture<EditemploienseignallPage>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(EditemploienseignallPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
