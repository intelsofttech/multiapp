import { ComponentFixture, TestBed } from '@angular/core/testing';
import { SaisiepresenceensPage } from './saisiepresenceens.page';

describe('SaisiepresenceensPage', () => {
  let component: SaisiepresenceensPage;
  let fixture: ComponentFixture<SaisiepresenceensPage>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(SaisiepresenceensPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
